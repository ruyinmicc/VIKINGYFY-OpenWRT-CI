# CI 项目
name: ZNM2-AX18

# CI 计划
on:
  # 自动编译：当 Auto-Clean 运行完成后
  workflow_run:
    workflows: ["Auto-Clean"]
    types:
      - completed
  # 手动编译
  workflow_dispatch:
    inputs:
      PACKAGE:
        description: '手动调整插件包，多个请用\n符号隔开。'
        required: false
        type: string
      TEST:
        description: '仅输出配置文件，不编译固件。'
        default: 'false'
        required: false
        type: boolean

# CI 权限
permissions: write-all

# CI 任务
jobs:
  config:
    name: ${{matrix.CONFIG}}
    strategy:
      fail-fast: false
      matrix:
        CONFIG: [ZNM2-AX18-WIFI]
        SOURCE: [VIKINGYFY/immortalwrt]
        BRANCH: [main]

    # 调用公用编译核心 WRT-CORE-LITE.yml
    # 注意：这里引用的文件名必须和你保存的第二个文件名完全一致（不含 .yml 后缀）
    uses: ./.github/workflows/WRT-CORE-LITE.yml
    with:
      # 编译配置
      WRT_CONFIG: ${{matrix.CONFIG}}
      # 默认主题
      WRT_THEME: aurora
      # 默认主机名
      WRT_NAME: OWRT
      # 默认 WIFI 名称
      WRT_SSID: OWRT
      # 默认 WIFI 密码
      WRT_WORD: 12345678
      # 默认地址
      WRT_IP: 192.168.10.1
      # 默认密码
      WRT_PW: 无
      # 源码链接
      WRT_REPO: https://github.com/${{matrix.SOURCE}}.git
      # 源码分支
      WRT_BRANCH: ${{matrix.BRANCH}}
      # 源码名称
      WRT_SOURCE: ${{matrix.SOURCE}}
      # 插件调整
      WRT_PACKAGE: ${{inputs.PACKAGE}}
      # 仅输出配置文件
      WRT_TEST: ${{inputs.TEST}}
